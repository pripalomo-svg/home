# Contratos assinados (PDF)

Coloque aqui:
- PDFs baixados do formulário (`Contrato_Assinado_*.pdf`)
- Textos de e-mail do EmailJS (`.txt` / `.eml`) começando com `NOVO CONTRATO DE PACIENTE FINALIZADO`

Depois rode:

```bash
python3 organizacao/gerar_contratos_assinados.py
```
